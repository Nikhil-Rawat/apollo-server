type User {
  _id: ID
  name: String!
  email: String!
  role: String!
  originalId: ID!
  createdAt: String!
}

input Payload {
  email: String!
  password: String!
}

type LoginResponse {
  status: Int!
  message: String!
  data: String
}
